# this is a syntactical shortcut for an if else statement

value = 1 < 2 ? "Yes, it is" : "No, it is not"
puts value

str_compare = "yes".downcase == "Yes" ? "The two are the same" 
: "The two are not the same"
puts str_compare